#include<stdio.h>
struct book
{
	char a[20];
	int num;
	int sold;
};
struct information(); // a fuction of type structure
int main()
{
	struct book *p;
	p=struct information();
	printf("you have entered the following information %s,%d,%d\n",(*p).a,(*p).num,(*p).sold);
	return 0;
}
struct information()
{
	struct book *p=(struct *)malloc(1*size of (struct book);
	printf("enter your book name\n");
	scanf("%s",p.

	

