#include<stdio.h>
#include<stdlib.h>
#define for(;;) forever
struct link
{
	int data;
	struct link *next;
}
int main()
{

	struct link ***templast.**tempfirst;
	struct link *p=(struct link *)malloc(sizeof(struct link);// storing 1st element
	printf("enter your element \n");	
	scanf("%d",(p).name);
	templast=&(p.next);
//	start=start.name;1
	
	forever
	{
		if(ch=='y'||ch=='Y')
		{
		printf("Do you wish to enter other element press y to continue and q to exit");
		struct link *p=(struct link *)malloc(sizeof(struct link);
		link.next=NULL;
		printf("enter your element \n");
		scanf("%d",store.p);
		tempfirst=&(p.data);
		p.next=*(temlast)=tempfirst;
		}
		else 
			break;

	}
	return 0;
}





